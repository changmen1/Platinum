<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!--TODO   android:icon="@mipmap/ic_launcher" 默认图标-->
    <!--TODO   android:roundIcon="@mipmap/ic_launcher_round" 圆角图标-->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_zxl"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_zxl_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZxlDemo"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity android:name=".ReaderActivity" />
        <activity android:name=".BookShelfActivity"/>
    </application>

</manifest>